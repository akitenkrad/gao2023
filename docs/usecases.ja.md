# ユースケース

本プロジェクトでできることと，詳細ドキュメントへの導線．

## 1. 単一 S³ シミュレーションの実行

有向フォローグラフ上で LLM エージェントを駆動し，集団レベル伝播の創発を観察する．

```bash
cargo run --release -- run --network ba --population 100 --rounds 20 --seed 42
uv run s3-tools visualize
```

全フラグは [CLI](cli.ja.md)，図は [可視化](visualization.ja.md) を参照．

## 2. 実行のコスト 0 再現 (キャッシュ決定論)

ウォーム `.llm_cache/` で **同一** コマンドを再実行すると，同一 LLM 応答が再生され (cache-hit ≈ 100%)，決定論的コアが bit 安定かつ高速になる．実行内容の確認:

```bash
uv run s3-tools show-experiment-settings
```

[アーキテクチャ › 二層決定論](architecture.ja.md#二層決定論) を参照．

## 3. オフライン検証 (ライブ LLM 不要)

Ollama/OpenAI の無いサンドボックスや CI で，mock 駆動スモークとテストを実行する．

```bash
cargo test                                   # mock 駆動; ライブ LLM 不要
cargo run --release --example mock_smoke -- results
uv run s3-tools visualize
```

mock (`socsim-llm::mock::ScriptedClient`) はスクリプト化された感情/態度/行動決定を返し，パイプライン全体 (配送 → 知覚 → 伝染 → 指標 → CSV/JSON) を駆動する．

## 4. 感度分析 (network × population)

ネットワーク種別と人口規模を走査し，トポロジと規模が伝播へ与える影響を見る．

```bash
cargo run --release -- sweep --network er,ws,ba --population-values 50,100,200 --rounds 20 --runs 3 --seed 42
uv run s3-tools visualize-sweep
```

期待 (定性的): BA (スケールフリー) はハブ経由で情報が最速で拡散し，人口規模が大きいほど集団レベル割合が安定する．

## 5. headline 結果の一括再現と古典ベースライン比較

S³ を 1 回実行し，伝播観測量を論文の定性帯と照合し，同一の有向網上で 4 つの古典的拡散・意見ダイナミクスと比較する:

```bash
# オフライン (live LLM 不要): scripted mock クライアントがパイプラインを端から端まで再現する
cargo run --release -- reproduce --mock --quick --seed 42
uv run s3-tools reproduce
```

run ディレクトリの `events.jsonl` が observed-vs-paper チェック (態度上昇・カスケード成長・行動採用・感情分布の MSED 代理・態度の Pearson 相関代理) を PASS / off 判定付きで記録し，`metrics.csv` が観測量と LT / IC / Voter / DeGroot の系列を持つ．単独のベースラインは `cargo run -- baseline --model {lt|ic|voter|degroot}` で実行できる．

## 6. LLM 駆動 Perception

既定では Perception は規則ベースの決定論的影響スコアでメッセージをランク付けする (LLM 呼び出し 0 回)．`--llm-perception` を渡すと候補メッセージのランキングを LLM に委ねる:

```bash
cargo run --release -- run --network ba --population 20 --rounds 3 --seed 42 --llm-perception
```

LLM 駆動経路は Contagion 呼び出しと同一のクライアント・キャッシュを共有し，LLM が有効な番号を返さなければ規則ベーススコアへフォールバックする．初期態度の LLM プロンプトも `prompts.rs` に用意してある．

---
*This file was generated by Claude Code.*
